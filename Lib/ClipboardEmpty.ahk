; ===================================================================================
; 				                  ФУНКЦИЯ ОЧИСТКИ БУФЕРА ОБМЕНА
; ===================================================================================
ClipboardEmpty(t:=100) {
  local empty
  Clipboard := empty
  If (t > 0) {
    Sleep,%t%
  }
}
